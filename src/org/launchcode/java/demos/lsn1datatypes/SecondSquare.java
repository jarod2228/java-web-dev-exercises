package org.launchcode.java.demos.lsn1datatypes;

public class SecondSquare {
}

               arrayList<MenuItem> Menu
- consists of several MenuItem items
- displays when it was last updated

                methods
addItem(MenuItem item) {
    menuName.add(item);
        }
removeItem(MenuItem item) {
    menuName.remove(item);
        }
printMenu() {
    use a for each loop to print the menu items from the array list
    system.out.println(menuName[i]);
        }